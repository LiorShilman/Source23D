<UserControl x:Class="CodeFlow3D.Views.CodePreviewPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:avalon="http://icsharpcode.net/sharpdevelop/avalonedit"
             Background="{StaticResource BgSurfaceBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="140" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="10,8" BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,0,0,1">
            <DockPanel>
                <TextBlock DockPanel.Dock="Right" Text="{Binding Language}" Style="{StaticResource MutedText}" VerticalAlignment="Center" />
                <TextBlock Style="{StaticResource SubHeaderText}">
                    <Run Text="Code Preview" />
                    <Run Text=" - " Foreground="{StaticResource TextMutedBrush}" />
                    <Run Text="{Binding FileName, Mode=OneWay}" Foreground="{StaticResource AccentCyanBrush}" />
                </TextBlock>
            </DockPanel>
        </Border>

        <!-- Code Editor -->
        <avalon:TextEditor x:Name="CodeEditor" Grid.Row="1"
                           IsReadOnly="True"
                           ShowLineNumbers="True"
                           FontFamily="Consolas"
                           FontSize="15"
                           Background="#0D0E14"
                           Foreground="#E8EAFF"
                           LineNumbersForeground="#5A6080"
                           WordWrap="False"
                           HorizontalScrollBarVisibility="Auto"
                           VerticalScrollBarVisibility="Auto"
                           Padding="8,4" />

        <!-- Stats Header -->
        <Border Grid.Row="2" Padding="10,6" BorderBrush="{StaticResource GlassBorderBrush}" BorderThickness="0,1,0,0">
            <TextBlock Text="Flow Statistics" Style="{StaticResource SubHeaderText}" />
        </Border>

        <!-- Stats -->
        <Border Grid.Row="3" Padding="10,6" Background="{StaticResource BgCardBrush}">
            <TextBlock Text="{Binding StatsText}" Style="{StaticResource MonoText}" FontSize="11"
                       TextWrapping="Wrap" />
        </Border>
    </Grid>
</UserControl>
